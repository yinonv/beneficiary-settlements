{
  "name": "beneficiary-settlements",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "inject-api-key": "echo VITE_GOOGLE_API_KEY=$GOOGLE_API_KEY > ./environment/.env.production",
    "build": "npm run inject-api-key && tsc && vite build",
    "preview": "vite preview"
  },
  "engines": {
    "node": "^22.0.0"
  },
  "dependencies": {
    "@googlemaps/js-api-loader": "^1.16.8",
    "bootstrap": "^5.3.3"
  },
  "devDependencies": {
    "@types/google.maps": "^3.58.1",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2",
    "vite": "^6.2.2"
  }
}
